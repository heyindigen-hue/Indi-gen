You are WORKER Claude on PC2. Major rebrand: change the app name from "Indi-gen" to "LeadHangover" everywhere + swap brand assets to a new orange flower logo.

## Brand Direction

**Name:** LeadHangover
**Tagline:** "Wake up to better leads"
**Primary color (brand orange):** `#FF4716` (hex)
**Secondary (cream, for logo on dark):** `#F5E6D3`
**Dark theme stays** — only swap the accent from indigo `#7170FF` to brand orange `#FF4716`
**Display font:** Fraunces (serif italic for headlines — matches the brand aesthetic the user provided)
**UI font:** Inter (unchanged)

## Your Task

### 1. Create SVG logo file: `shared/brand/logo.svg`
An 8-petal abstract flower/asterisk shape, slightly rotated. Each petal is an ellipse arranged in a radial pattern. Cream color on transparent/orange. Here's the SVG to use verbatim:

```xml
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 200 200" fill="none">
  <g transform="translate(100 100)">
    <g fill="#F5E6D3">
      <ellipse cx="0" cy="-55" rx="14" ry="42" />
      <ellipse cx="0" cy="-55" rx="14" ry="42" transform="rotate(45)" />
      <ellipse cx="0" cy="-55" rx="14" ry="42" transform="rotate(90)" />
      <ellipse cx="0" cy="-55" rx="14" ry="42" transform="rotate(135)" />
      <ellipse cx="0" cy="-55" rx="14" ry="42" transform="rotate(180)" />
      <ellipse cx="0" cy="-55" rx="14" ry="42" transform="rotate(225)" />
      <ellipse cx="0" cy="-55" rx="14" ry="42" transform="rotate(270)" />
      <ellipse cx="0" cy="-55" rx="14" ry="42" transform="rotate(315)" />
      <circle cx="0" cy="0" r="10" fill="#FF4716" />
    </g>
  </g>
</svg>
```

Also create:
- `shared/brand/logo-dark.svg` — same but with `#F5E6D3` petals (for dark backgrounds — the default)
- `shared/brand/logo-light.svg` — same shape but petals are `#FF4716` (for light backgrounds)
- `shared/brand/logo-mark.svg` — just the petals on transparent (square 512x512)
- `shared/brand/logo-orange-bg.svg` — cream petals on solid orange `#FF4716` full-bleed square (for app icon / social OG)

### 2. Generate PNG assets from the SVGs

Use `sharp` or `puppeteer` to render the PNGs. Easy approach: write a small Node script.

```
cd C:\Users\Indigen-Server-2\Desktop\Indi-gen
npm install -D sharp
node shared/brand/generate-pngs.js
```

Create `shared/brand/generate-pngs.js`:
```javascript
const sharp = require('sharp');
const fs = require('fs');
const path = require('path');

const dir = path.join(__dirname);
const outs = [
  { src: 'logo-orange-bg.svg', out: 'icon-512.png', size: 512 },
  { src: 'logo-orange-bg.svg', out: 'icon-1024.png', size: 1024 },
  { src: 'logo-orange-bg.svg', out: 'adaptive-icon.png', size: 1024 },
  { src: 'logo-dark.svg', out: 'logo-dark-256.png', size: 256 },
  { src: 'logo-light.svg', out: 'logo-light-256.png', size: 256 },
  { src: 'logo-orange-bg.svg', out: 'splash.png', size: 2048 },
  { src: 'logo-mark.svg', out: 'favicon.png', size: 64 },
];
(async () => {
  for (const o of outs) {
    await sharp(path.join(dir, o.src)).resize(o.size, o.size).png().toFile(path.join(dir, o.out));
    console.log('wrote', o.out);
  }
})();
```

Run it, verify PNGs are created.

### 3. Update server theme accent

In `server/src/index.ts` nothing to change.

In `db/seed.sql`, update seed settings:
```sql
UPDATE app_settings SET value='LeadHangover' WHERE key='brand_name';
UPDATE app_settings SET value='Wake up to better leads' WHERE key='brand_tagline';
UPDATE app_settings SET value='#FF4716' WHERE key='brand_primary_color';
UPDATE app_settings SET value='/brand/logo-light-256.png' WHERE key='brand_logo_light_url';
UPDATE app_settings SET value='/brand/logo-dark-256.png' WHERE key='brand_logo_dark_url';
UPDATE app_settings SET value='/brand/favicon.png' WHERE key='brand_favicon_url';
UPDATE app_settings SET value='/brand/icon-1024.png' WHERE key='brand_og_image_url';
UPDATE app_settings SET value='support@leadhangover.com' WHERE key='brand_support_email';
```

(Add the UPDATE statements at the end of the seed file so re-runs pick up the new brand.)

Also update the default UI manifest insert — change:
- `"brand":{"name":"Indi-gen",...}` → `"name":"LeadHangover","tagline":"Wake up to better leads","primary":"#FF4716"`
- `"ai_context_pitch"` setting: update "Indigen Services" → "LeadHangover"

### 4. Rename across package.jsons
- `server/package.json`: name = `leadhangover-server`
- `admin/package.json`: name = `leadhangover-admin`
- `mobile/package.json`: name = `leadhangover-mobile`
- `scraper-service/app/main.py` FastAPI title: `"LeadHangover Scraper Service"`

### 5. Mobile app rebrand (big one)

**`mobile/app.json`**:
```json
{
  "expo": {
    "name": "LeadHangover",
    "slug": "leadhangover",
    "scheme": "leadhangover",
    "owner": "...",
    "icon": "./assets/icon.png",
    "splash": { "image": "./assets/splash.png", "backgroundColor": "#FF4716", "resizeMode": "contain" },
    "android": {
      "package": "com.leadhangover.app",
      "adaptiveIcon": { "foregroundImage": "./assets/adaptive-icon.png", "backgroundColor": "#FF4716" }
    },
    "ios": {
      "bundleIdentifier": "com.leadhangover.app"
    },
    ...
  }
}
```

Copy PNG assets:
- `shared/brand/icon-1024.png` → `mobile/assets/icon.png`
- `shared/brand/splash.png` → `mobile/assets/splash.png`
- `shared/brand/adaptive-icon.png` → `mobile/assets/adaptive-icon.png`
- `shared/brand/favicon.png` → `mobile/assets/favicon.png`

**`mobile/constants/defaultManifest.json`** — update brand subtree:
```json
"brand": {"name":"LeadHangover","tagline":"Wake up to better leads","primary":"#FF4716","logo_light":"","logo_dark":""}
```

Also update `mobile/lib/theme.ts` palette "graphite":
```typescript
graphite: {
  bg: '#08090B', card: '#111113', border: '#1F1F23',
  text: '#F7F8F8', muted: '#8A8F98',
  primary: '#FF4716',       // was #7170FF — rebrand to LeadHangover orange
  primaryFg: '#FFFFFF',
  success: '#4CB782', warning: '#F2C94C', destructive: '#E5484D',
},
```

### 6. Admin panel rebrand

**`admin/index.html`**:
- `<title>LeadHangover Admin</title>`
- Add `<link rel="icon" href="/brand/favicon.png">`

**`admin/src/index.css`**:
- Update `--primary: 240 100% 72%` (indigo) → `--primary: 13 100% 54%` (orange HSL for #FF4716)
- Update `--ring` to match
- Light mode primary: `--primary: 13 100% 48%`

**`admin/src/components/layout/Sidebar.tsx`**:
- Replace any "Indi-gen" text with "LeadHangover"
- Logo: `<img src="/brand/logo-dark-256.png" alt="LeadHangover" />` in the header area

**`admin/src/pages/Login.tsx`**:
- Heading: "LeadHangover" (display this in Fraunces italic serif if the font loads)

Copy brand assets to `admin/public/brand/`:
```
cp shared/brand/logo-dark-256.png admin/public/brand/
cp shared/brand/logo-light-256.png admin/public/brand/
cp shared/brand/favicon.png admin/public/brand/
cp shared/brand/icon-512.png admin/public/brand/
```

Also add Fraunces font to `admin/index.html`:
```html
<link href="https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght@0,9..144,400;0,9..144,600;1,9..144,400;1,9..144,600&display=swap" rel="stylesheet">
```

Update `admin/tailwind.config.js` fontFamily:
```js
fontFamily: {
  sans: ['Inter', 'system-ui', 'sans-serif'],
  mono: ['Geist Mono', 'ui-monospace'],
  display: ['Fraunces', 'serif'],
}
```

Use `font-display italic` in key hero text (Login title, brand header in sidebar).

### 7. Server brand route

Make sure `server/src/routes/sdui.ts` serves the brand assets — if there's a `/api/public/brand` endpoint, it should return:
```json
{
  "name": "LeadHangover",
  "tagline": "Wake up to better leads",
  "primary": "#FF4716",
  "logo_light_url": "https://leadgen.indigenservices.com/brand/logo-light-256.png",
  "logo_dark_url": "https://leadgen.indigenservices.com/brand/logo-dark-256.png",
  "favicon_url": "https://leadgen.indigenservices.com/brand/favicon.png"
}
```

Add a static route in `server/src/index.ts`:
```typescript
import path from 'path';
app.use('/brand', express.static(path.resolve(__dirname, '../../shared/brand')));
```

### 8. README.md — update name + description
Change title from "Indi-gen" to "LeadHangover", update description to match tagline.

### 9. docker-compose.yml — rename containers
Change `indigen-*` container names to `leadhangover-*`.

### 10. Grep sweep for any remaining "Indi-gen" or "indigen"
```bash
cd C:\Users\Indigen-Server-2\Desktop\Indi-gen
grep -riIn "indi-gen\|indigen" --include="*.ts" --include="*.tsx" --include="*.json" --include="*.md" --include="*.yml" --include="*.conf" . | grep -v node_modules | grep -v dist | grep -v ".next"
```
Review each hit and replace with LeadHangover (or leadhangover lowercase for package names).

IMPORTANT exceptions — keep as-is:
- `indigenservices5@gmail.com` (user's email, don't change)
- GitHub remote URL `heyindigen-hue/Indi-gen` (keep — that's the repo name)
- Path `C:\Users\Indigen-Server-2\Desktop\Indi-gen` (filesystem path, don't rename dir)
- Any comment referencing "Indigen Services" as the company name (that's the company, LeadHangover is the product — both can coexist)

### 11. Commit
```
git add -A
git commit -m "rebrand: Indi-gen → LeadHangover (orange flower logo, brand palette, assets across all surfaces)"
git push origin master
```

Report: files touched, grep results, SHA.

## Guardrails
- Do NOT rename the folder `Indi-gen` (too invasive, git history ties to it).
- Do NOT break package-lock.json by renaming too aggressively.
- Do NOT run expo start / dev servers.
- Do NOT fail on sharp install on Windows — if it fails, skip PNG generation, just commit the SVGs and note the PNGs need to be generated later.
