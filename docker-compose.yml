version: '3.9'

services:
  postgres:
    image: postgres:16
    container_name: leadhangover-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: indigen
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - '5432:5432'
    networks:
      - leadhangover

  redis:
    image: redis:7-alpine
    container_name: leadhangover-redis
    restart: unless-stopped
    ports:
      - '6379:6379'
    networks:
      - leadhangover

  server:
    build:
      context: ./server
    container_name: leadhangover-server
    restart: unless-stopped
    env_file: .env
    environment:
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/indigen
      REDIS_URL: redis://redis:6379
    depends_on:
      - postgres
      - redis
    networks:
      - leadhangover
    expose:
      - '3001'

  scraper:
    build:
      context: ./scraper-service
    container_name: leadhangover-scraper
    restart: unless-stopped
    env_file: .env
    environment:
      REDIS_URL: redis://redis:6379
    depends_on:
      - redis
    networks:
      - leadhangover
    expose:
      - '8000'

  admin:
    build:
      context: ./admin
    container_name: leadhangover-admin
    restart: unless-stopped
    networks:
      - leadhangover
    expose:
      - '80'

  nginx:
    image: nginx:alpine
    container_name: leadhangover-nginx
    restart: unless-stopped
    ports:
      - '80:80'
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - server
      - scraper
      - admin
    networks:
      - leadhangover

volumes:
  postgres_data:

networks:
  leadhangover:
    driver: bridge
